{
  "name": "@xeron688/xiaohongshu-mcp",
  "version": "1.0.4",
  "description": "MCP Server for generating Xiaohongshu content outlines and images",
  "main": "build/index.js",
  "type": "module",
  "bin": {
    "xiaohongshu-mcp": "cli.js"
  },
  "files": [
    "build",
    "cli.js",
    "data",
    "LICENSE",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node build/index.js",
    "dev": "tsc && node build/index.js",
    "test": "vitest",
    "test:run": "vitest --run",
    "prepublishOnly": "npm run build",
    "release:patch": "npm version patch && npm publish && git push origin main --follow-tags",
    "release:minor": "npm version minor && npm publish && git push origin main --follow-tags",
    "release:major": "npm version major && npm publish && git push origin main --follow-tags"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Xeron2000/redBookMCP.git"
  },
  "keywords": [
    "mcp",
    "xiaohongshu",
    "content-generation",
    "ai"
  ],
  "author": "xeron688",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/uuid": "^9.0.0",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0",
    "fast-check": "^3.0.0"
  }
}
